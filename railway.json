{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && NODE_ENV=production BASE_PATH=/ pnpm --filter estate-agent build && pnpm --filter api-server build"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "restartPolicyType": "ON_FAILURE"
  }
}
